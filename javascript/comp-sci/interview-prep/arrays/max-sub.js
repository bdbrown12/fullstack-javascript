function maxSub(arr) {
    //given an array find the subarray that has the maximum sum and return its sum



    //sort the array?

    //brute force
    let currentSum = 0;
    let maxSum = 0;

    for(let i = 0; i < arr.length; i++){
        for(let j = i + 1; j < arr.length - 1; j++){
            //if
        }
    }

    //return the array in unsorted form

    //add up the whole array to figure out the 

}

let arr = [2, 3, -8, 7, -1, 2, 3]

maxSub(arr)