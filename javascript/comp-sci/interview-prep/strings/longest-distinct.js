let string = 'longest'

//find the longest substring with all distinct characters

// how do i find a substring

// iterate over the string
for(let i = 0; i < string.length; i++){

    newString = '';

    // loop through a string

    // check if any characters repeat

    // if characters repeat && string is longer than previous longest string
        //return string
    // else
        //repeat loop

    console.log(string[i]);
}